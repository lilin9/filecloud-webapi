namespace Domain {
    /// <summary>
    /// 缓存操作相关
    /// </summary>
    public class CacheHelper {
        public static void SaveToCache<T>(string key, T msg, DateTimeOffset expiration) {
            
        }
    }
}
